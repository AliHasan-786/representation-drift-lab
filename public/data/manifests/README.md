# Public provenance manifests

These compact manifests are generated from completed run manifests and contain the stable, non-secret fields required to trace every published chart. Local checkpoint and embedding files remain ignored.

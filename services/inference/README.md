# Optional inference service

Live image inference is separated from the static portfolio experience. This service will be implemented after benchmark model selection and must enforce upload validation, transient processing, rate limits, timeouts, and an offline fallback.
